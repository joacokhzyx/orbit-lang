# Orbit syntax guide

The maintained language documentation is the [Orbit language reference](LANGUAGE_REFERENCE.md).
It covers functions, bindings, control flow, arrays, HTTP routes, SQLite, modules,
and the current release limitations.
