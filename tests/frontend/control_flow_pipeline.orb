import "../../compiler/frontend/frontend.orb"

fn main() {
    val source = "fn choose(flag: bool) -> int { if flag { return 1 } else { return 2 } }"
    print(compileFrontendSource("control-flow", source))
}
