import "../../compiler/frontend/frontend.orb"

fn main() {
    val source = "fn unresolved() { return absent }"
    val output = compileFrontendSource("unresolved", source)
    if output == "" {
        print("unresolved type suppressed TIR")
    }
}
